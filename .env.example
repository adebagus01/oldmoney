# Local dev default — matches the Docker command in the README.
# In Vercel, set this under Project Settings → Environment Variables instead.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oldmoney"
